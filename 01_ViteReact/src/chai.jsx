/*
  // Components/Functions name first letter should be in capital, so it can work properly. If you use lower first letter then it will not work. Your component file name can be in lower case but for convention use Capitalize name.
*/

function Chai(){
  return (
    <h3> Chai with Sanjay </h3>
  )
};

export default Chai;