import Chai from './Chai.jsx'

function App() {

  return (
    /*
      // Here <> </> called 'fragment' act as a <div> </div> element. So we can easily store multiple element inside it. If we don't use it then we can only story a single element in .jsx file. Vite force us to use .jsx file extension to work properly.
    */
   
    <>
      <h1> React with Vite </h1>
      <Chai/>
    </>
  )
}

export default App
